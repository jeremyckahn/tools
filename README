Make sure this directory is in your user's home directory. (~/)

Make sure it is called "Tools." (~/Tools)

Make sure you copy the .bash_profile file directly into you user's root directory (~/.bash_profile)

Make sure you start a new terminal session to see the new commands.

Make sure you enjoy.  :)
